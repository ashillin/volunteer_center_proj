<h1>{{title}}</h1>
{{#if currentUser}}
<a href="/roles/form">Add a new Role</a>
{{/if}}
<ul>
 {{#each roles}}
   <li><a href="/roles/show/{{this.id}}">{{this.id}} - {{this.roleName}}</a>
    {{#if currentUser}} {{#if (eq currentUser.volunteerId site.volunteerId)}}<a href="/sites/edit?id={{siteId}}">Edit</a>{{/if}}{{/if}}</li>
 {{/each}}
</ul>